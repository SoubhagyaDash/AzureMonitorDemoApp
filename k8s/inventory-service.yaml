apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: otel-demo
  labels:
    app: inventory-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
      - name: inventory-service
        image: acroteldemodev1l009e.azurecr.io/inventory-service:latest
        ports:
        - containerPort: 3001
        env:
        - name: APPLICATIONINSIGHTS_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: appinsights-connection
              key: connection-string
        - name: OTEL_SERVICE_NAME
          value: "inventory-service"
        - name: OTEL_SERVICE_VERSION
          value: "1.0.0"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: otel-demo
spec:
  selector:
    app: inventory-service
  ports:
  - port: 3001
    targetPort: 3001
  type: ClusterIP
