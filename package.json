{
  "name": "otel-demo-frontend-server",
  "version": "1.0.0",
  "description": "Express server to serve OpenTelemetry Demo Frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd services/frontend && npm run build && cd ../..",
    "postinstall": "cd services/frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}